# +==============================================================================â•—
# | [WARRIOR] ORFEAS AI PRODUCTION REQUIREMENTS [WARRIOR] |
# | ORFEAS AI - Production-Ready Deployment |
# | ONLY THE BEST FOR PRODUCTION |
# +==============================================================================

# [SECURE] PRODUCTION WSGI SERVER (DO NOT USE FLASK DEV SERVER!)
# Choose ONE of the following:

# Option 1: Gunicorn with Eventlet (Recommended for SocketIO)
gunicorn==21.2.0
eventlet==0.33.3

# Option 2: Uvicorn with WebSockets support (Alternative)
# uvicorn[standard]==0.24.0

# [LAUNCH] INSTALLATION INSTRUCTIONS:
#
# 1. Install production server:
#    pip install -r requirements-production.txt
#
# 2. Start with Gunicorn:
#    gunicorn --worker-class eventlet --workers 1 --bind 0.0.0.0:5000 backend.main:app
#
# 3. OR start with Uvicorn:
#    uvicorn backend.main:app --host 0.0.0.0 --port 5000 --workers 1
#
# 4. Set environment variable:
#    export PRODUCTION=true
#    (Windows: $env:PRODUCTION="true")
#
# 5. Ensure SECRET_KEY is set:
#    export SECRET_KEY="your-secure-random-secret-key-here"
#
# [WARN] CRITICAL PRODUCTION CHECKLIST:
#
# [OK] SECRET_KEY set (NOT the default value!)
# [OK] PRODUCTION=true environment variable set
# [OK] Using gunicorn or uvicorn (NOT Flask development server)
# [OK] CORS_ORIGINS configured (not wildcard *)
# [OK] ENABLE_RATE_LIMITING=true
# [OK] ORFEAS_DEBUG=false
# [OK] All model paths validated
# [OK] GPU memory limits configured appropriately
# [OK] Monitoring stack running (Prometheus + Grafana)
# [OK] SSL/TLS configured (HTTPS)
# [OK] Firewall rules configured
# [OK] Backup strategy in place
#
# [STATS] MONITORING:
# - Prometheus endpoint: http://localhost:5000/metrics
# - Grafana dashboard: http://localhost:3001
#
#  SECURITY:
# - Never commit .env files with production secrets
# - Use environment variables or secure key management
# - Rotate SECRET_KEY regularly
# - Monitor logs for security events
# - Keep dependencies updated
#
# [LAUNCH] PERFORMANCE:
# - Use reverse proxy (Nginx, Caddy, Traefik)
# - Enable gzip compression
# - Configure appropriate worker count (CPU cores * 2 + 1)
# - Set up load balancing for high traffic
# - Monitor memory usage and GPU utilization
#
# PRODUCTION EXCELLENCE ONLY!
